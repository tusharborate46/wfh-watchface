import nodemailer from 'nodemailer';
export async function sendUnknownFaceAlert({employee, manager}){if(process.env.ALERT_WEBHOOK_URL){await fetch(process.env.ALERT_WEBHOOK_URL,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'UNKNOWN_FACE',employee,manager})});return;}if(process.env.SMTP_HOST){const t=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});await t.sendMail({from:process.env.ALERT_FROM,to:[employee.email,manager?.email].filter(Boolean),subject:'Unknown face alert',text:`An unknown face was detected for ${employee.name}. No image was captured or stored.`});}}
