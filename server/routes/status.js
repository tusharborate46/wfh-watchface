import express from 'express';import { query } from '../db.js';import { auth, canAccessEmployee } from '../middleware/auth.js';import { sendUnknownFaceAlert } from '../services/alertService.js';
const r=express.Router();r.use(auth);const allowed=['VERIFIED','AWAY','UNKNOWN_FACE','CAMERA_ERROR'];
r.post('/',async(req,res)=>{const {employeeId,status,timestamp}=req.body;if(!canAccessEmployee(req,employeeId))return res.status(403).json({error:'Forbidden'});if(!allowed.includes(status))return res.status(400).json({error:'Invalid status'});const {rows}=await query('insert into status_logs(employee_id,status,checked_at) values($1,$2,$3) returning *',[employeeId,status,timestamp||new Date()]);if(status==='UNKNOWN_FACE'){await query('insert into alerts(employee_id,triggered_at) values($1,$2)',[employeeId,timestamp||new Date()]);const people=await query('select e.*,m.email manager_email,m.name manager_name from employees e left join employees m on e.manager_id=m.id where e.id=$1',[employeeId]);sendUnknownFaceAlert({employee:people.rows[0],manager:{email:people.rows[0]?.manager_email,name:people.rows[0]?.manager_name}}).catch(console.error);}res.status(201).json(rows[0]);});
r.get('/:employeeId',async(req,res)=>{if(!canAccessEmployee(req,req.params.employeeId))return res.status(403).json({error:'Forbidden'});const {rows}=await query("select * from status_logs where employee_id=$1 and checked_at::date=current_date order by checked_at desc",[req.params.employeeId]);res.json(rows);});
export default r;
