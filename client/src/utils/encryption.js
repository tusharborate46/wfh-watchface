/**
 * encryption.js (Client utility stub)
 * Face embedding encryption is handled securely server-side in encryptionService.js
 * to ensure cryptographic keys are not exposed to the client.
 */
export function getEncryptionStatus() {
  return 'Server-side AES-256-GCM encryption enabled';
}
