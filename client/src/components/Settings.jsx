import { useState } from 'react';import { api } from '../utils/api';
export default function Settings(){const [msg,setMsg]=useState('');return <main className="p-8"><div className="card max-w-xl"><h1 className="text-3xl font-black">Privacy settings</h1><p className="my-4 text-slate-300">Delete your enrollment embedding at any time. This removes the encrypted vector used for verification.</p><button className="btn bg-red-500 text-white hover:bg-red-400" onClick={()=>api('/api/enrollment/me',{method:'DELETE'}).then(()=>setMsg('Enrollment deleted.'))}>Delete enrollment</button><p className="mt-4">{msg}</p></div></main>}
